# AgentForge Harness — 工作区地图

> 模型决定上限，Harness 决定底线。

## 铁律

🔴 完整铁律文件: `~/.codex/rules/IRON_LAWS.md`（18条，运行时自动加载）

## 技能索引

| 技能 | 用途 |
|---|---|
| `agentforge-fix` | 全链路Bug修复 |
| `agentforge-analyze` | 根因分析 |
| `agentforge-test` | Playwright回归测试 |
| `agentforge-verify` | 最终验收 |
| `agentforge-db-review` | 数据库审查 |
| `agentforge-archive` | 三重归档 |
| `harness-engineering` | Harness Engineering方法论 |

## 智能体

```
刘备(协调) → 诸葛亮(分析) → {关羽|赵云}(修复) → 荀彧(DB审查) → 张飞(测试) → 华佗(验收) → 陈琳(归档)
```

## 工作循环

```
Init → Plan → Implement → Verify → Cleanup
```
